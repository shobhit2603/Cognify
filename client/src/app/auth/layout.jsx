export const metadata = {
  title: "Authentication | Cognify",
  description: "Sign in or create an account to access Cognify.",
};

export default function AuthLayout({ children }) {
  return <>{children}</>;
}
